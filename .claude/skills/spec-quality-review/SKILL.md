---
name: spec-quality-review
description: spec PR を人間がレビューする前に、Kiro spec の requirements.md と design.md の記述品質、行数、設計理由、development-rules.md への準拠を確認するときに使う。
---

# Spec 品質レビュー

spec PR を人間がレビューする前に、対象 spec 一覧に含まれる各 spec の `requirements.md` と `design.md` がプロジェクトの spec 運用ルールに沿っているか確認する。
対象 spec が 1 件だけでも、依頼では必ずリストとして列挙される前提で読む。

## 読むファイル

1. 対象 spec 一覧に含まれる各 `.kiro/specs/<spec名>/requirements.md`
2. 対象 spec 一覧に含まれる各 `.kiro/specs/<spec名>/design.md`
3. `.kiro/steering/development-rules.md`
4. 必要に応じて `docs/requirements-spec.md`

## 確認観点

- `requirements.md` と `design.md` がそれぞれ最大150行以内に収まっているか
- 120行を超えるファイルがある場合、分割提案が必要か
- `requirements.md` にユーザーストーリーと受け入れ基準が明確に書かれているか
- `design.md` に、その spec 内で必要なインターフェース・コマンド・データ構造だけが自己完結して書かれているか
- `design.md` に「なぜその設計にしたか」が読み取れる設計判断の理由があるか
- 全体アーキテクチャや共通データモデルなど、`docs/requirements-spec.md` に置くべき内容が混ざっていないか
- requirements と design の間に矛盾、曖昧さ、未定義の用語がないか
- 複数 spec がある場合、spec 間に矛盾や責務の重複がないか

## 出力形式

レビュー結果は、次の順で簡潔に出す。

1. **指摘事項**: 重大度順に、ファイルパスと該当箇所を添えて書く
2. **確認できたこと**: 問題がなかった主要観点を短く書く
3. **提案**: 人間または Kiro に依頼すべき修正があれば具体的に書く
4. **Kiro への依頼プロンプト**: 修正が必要な場合、Kiro にそのまま渡せるプロンプトをコードブロックで書く

問題がない場合は、指摘事項なしと明記し、残るリスクや未確認範囲だけを書く。
修正が不要な場合、**Kiro への依頼プロンプト** は「なし」と書く。
Kiro への依頼プロンプト内にコードブロックや Markdown 例を含める場合は、外側のコードフェンスを内側より長くするなど、表示が崩れない形式にする。

Kiro への依頼プロンプトには、次を含める。

- 対象 spec 名
- 修正対象ファイル
- 修正してほしい内容
- 守るべき制約や参照すべきルール
- 完了条件

## PR へのコメント投稿

PR 番号または PR URL が指定されている場合は、`/github-operations` を参照してレビュー結果を対象 PR へ投稿する。PR が指定されていないローカル差分レビューでは投稿しない。

- レビュー種別の識別子には `spec-quality` を使う
- 「指摘事項」の各項目を、1 項目につき 1 件、該当ファイルの変更行へのインラインレビューコメントとして投稿する。指摘事項を通常コメントとして投稿してはならない
- 指摘ごとにスレッドを分け、PR 上で返信・解決できる状態にする
- 削除された要素、記述の欠落、複数ファイルにまたがる問題など、問題そのものを表す変更行がない場合も、最も関連する変更行を選んでインラインコメントとして投稿し、コメント本文でその行との関係と指摘対象の範囲を明記する
- 各指摘コメントには、重大度、ファイルパスと該当箇所、問題、spec 運用ルール上の根拠、修正案を含める
- 指摘投稿後に、結論、指摘件数と重大度、確認結果、マージへ進めるか、未解決事項だけを簡潔にまとめた総括コメントを 1 件、PR の通常コメントとして投稿する
- 指摘事項がない場合は、総括コメントだけを投稿し、「指摘事項なし」と明記する
- 確認できたことの詳細、提案の詳細、Kiro への依頼プロンプトは PR コメントに含めず、投稿後のチャット応答にのみ出力する
- 投稿後のチャット応答には、投稿先 PR、投稿したインライン指摘コメント数、総括コメントの投稿結果を記載する
- PR の head commit SHA を取得し、各コメント末尾に次の識別子を入れる
  - 指摘: `<!-- claude-review:spec-quality:<head SHA>:finding:<連番> -->`
  - 総括: `<!-- claude-review:spec-quality:<head SHA>:summary -->`
- 投稿前に既存のインラインレビューコメントと通常コメントを確認し、同じ識別子があるコメントは再投稿しない
